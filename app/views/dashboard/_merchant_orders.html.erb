<h1> Orders </h1>
<% @orders.each do |order| %>
  <p> Order Id: <%=link_to order.id, order_path(order, merchant_id: @merchant_id) %> </p>
  <p> Date Made:  <%= order.created_at %> </p>
  <p> Status:  <%= order.status %> </p>
<% end %>
